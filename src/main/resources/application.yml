server:
  port: 8080
spring:
  # Database
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/baseapi
    username: postgres
    password: root
  # Flyway
  flyway:
    enabled: true
  # JPA properties
#  jpa:
#    hibernate:
#      ddl-auto: update
management:
  endpoint:
    health:
      show-details: always
  health:
    db:
      enabled: 'true'
    defaults:
      enabled: 'true'

